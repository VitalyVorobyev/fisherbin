# S01 — Scaffold + public-surface truth pass

**Workstream:** memory, W2 · **Needs:** — · **Parallel with:** — · **Status:** done

## Goal

Seed the programme's durable memory (roadmap block, this directory) so every later session starts
from the same files in any harness. At the same time make the published public surface true: no
exported name that nothing accepts, every code string the portal publishes executes in a test, and
the execution backend is documented in the API guide. Done is decidable: the greps and tests in
"Done criteria" pass and the roadmap row reads `done`.

## Inputs

- `/Users/vitalyvorobyev/.claude/plans/i-suggest-to-stop-memoized-goose.md` (owner's plan, 2 Sep
  2026; the source of every packet in this directory).
- `docs/roadmap.md` (M11 block and the "Beyond 1.0" profiled-compile paragraph to correct).
- `src/scorequant/components.py` (`LinearProblem`, `LinearComponents.evaluate`).
- `tests/test_workflow.py` (the only tests using `LinearProblem`).
- `website/src/pages/docs.tsx` and `tests/test_portal_snippets.py` (the broken Door 2 snippet and
  its name-check-only test).
- `tests/test_docs_snippets.py` (the fixture-namespace execution machinery to reuse).
- `docs/api.md` (no Execution section before this session).
- `agenticresearch/manuscripts/README.md` (file table listing the two companion HTMLs).
- `agenticresearch/WORK/TEMPLATE.md` and `agenticresearch/PLAYBOOK.md` (style for packets and the
  session prompt).

## Deliverables

- `docs/roadmap.md`: `M12 — Consolidation programme` block before M11 with four workstream gates,
  the session table with a `Status` column, the cut list and the exit gate; M11's gate requires
  M12; the stale profiled-compile paragraph in "Beyond 1.0" rewritten (OP29's deployment-facing
  scalar remainder is closed by DS17, DS18 and DS19; no compile route to implement).
- `docs/programme/README.md` (orchestrator contract, delegation ladder, verification, session
  prompt) and packets `S01`–`S09`.
- `mkdocs.yml` `exclude_docs`: `programme/**` replaces `proposals/**`; `tests/test_readme.py`
  `_UNPUBLISHED_DIRS` likewise; `docs/playbook.md` wording; `docs/proposals/*` removed (the API
  proposal landed as ADR 0021/0022; the showcase proposal is now W4 gate text).
- `LinearProblem` and `LinearComponents.evaluate` deleted from `src/scorequant/components.py` and
  `src/scorequant/__init__.py`; `docs/reference/models.md` directive removed;
  `tests/test_workflow.py` rewritten on `evaluate_components` + `scores_from_components`; ADR
  0005 carries a one-line removal note; CHANGELOG "Removed".
- `website/src/pages/docs.tsx`: Door 2 snippet rewritten to `LinearComponents` +
  `IntegrationSource` + `LinearComponentScore`; `website/src/generated/portal-data.json`
  regenerated by `pnpm generate:data`.
- `tests/test_portal_snippets.py`: every `code:` string executed in a fixture namespace and
  asserted to produce a `PartitionResult` or `QuantizerResult`; the public-name check kept.
- `docs/api.md`: `execution=` in both task signatures and a new "Execution" section
  (`ExecutionConfig` fields, NumPy backend, results remember their execution, per-call override,
  backend-free artifacts).
- `agenticresearch/manuscripts/`: `doptimal_event_categorization_hep.html` and
  `scorequant_research_landscape_en.html` moved to `agenticresearch/archive/`; README table
  updated.
- Claude auto-memory note `consolidation-programme.md` pointing at this directory and the M12
  table (no content duplicated).

## Done criteria

- `grep -rn LinearProblem src docs website/src tests` returns only ADR 0005, CHANGELOG and the
  planning text under `docs/programme/`.
- `tests/test_portal_snippets.py` executes each `code:` string in `docs.tsx` and passes.
- `docs/api.md` contains a `## Execution` section naming `ExecutionConfig`, the NumPy backend and
  the `predict_scores(execution=...)` override.
- `docs/programme/README.md` and `S01`–`S09` exist; `mkdocs build --strict` excludes them.
- Full handoff gate and `cd website && pnpm validate` are green.
- Roadmap M12 table shows S1 `done`; this packet's Closing report is written.

## Delegation

| Task | Tier | Output |
|---|---|---|
| Inventory: `LinearProblem` / `.evaluate(` references, portal `code:` strings, execution mentions in `docs/api.md` | haiku (done inline by the orchestrator with greps) | file:line list |
| Door 2 snippet, Execution section, M12 roadmap block, programme README | fable | text in the files |
| `LinearProblem` removal: code, tests, ADR note, CHANGELOG | sonnet | diff + `test_workflow.py` results |
| Portal snippet rewrite, executed-snippet test, `portal-data.json` regeneration | sonnet | diff + `test_portal_snippets.py` results |
| Packets S02–S09 from the plan | sonnet | eight files |
| Mechanics: mkdocs/test_readme switch, proposals removal, manuscript archive | haiku (done inline) | git status |
| Full gate + `pnpm validate` | haiku | verbatim results |
| Closing report | orchestrator | this file |

## Verification

```bash
uv run ruff check . && uv run ruff format --check . && uv run ty check src
JAX_ENABLE_X64=1 MPLBACKEND=Agg uv run pytest -n auto
JAX_ENABLE_X64=0 MPLBACKEND=Agg uv run pytest tests/test_float32.py
uv build && uv run mkdocs build --strict
cd website && pnpm validate
python agenticresearch/py/registry.py validate
grep -rn LinearProblem src docs website/src tests   # only ADR 0005, CHANGELOG, docs/programme
```

## Open decisions

- Taken here unless the owner overrides: delete (not privatize) `LinearProblem`; M12 placed before
  M11 so the release gate depends on the programme; the Door 2 snippet uses a bounded
  `IntegrationSource` rather than a Monte Carlo sample, because that is the route with no hidden
  sampling step.

## Closing report

Session S1 ran on 3 September 2026 on branch `consolidation-s1-scaffold` (one Claude Code
session, three sonnet and one haiku subagents).

**Delivered.** The programme now has durable memory: the M12 block in `docs/roadmap.md` (four
workstream gates, the S1–S9 table, the cut list, the exit gate; M11's gate now requires M12) and
this directory (orchestrator contract with a copy-paste session prompt, nine packets). The stale
"Beyond 1.0" paragraph that waited on OP29 now records that DS17, DS18 and DS19 leave no profiled
compile route to implement. `docs/proposals/` is gone and `docs/programme/` took its place in the
MkDocs exclusion list and the front-door prose guard. On the public surface: `LinearProblem` and
`LinearComponents.evaluate` are deleted (ADR 0005 note, CHANGELOG "Removed"; the tests now go
through `evaluate_components` and `scores_from_components`); the portal's Door 2 snippet is a
real `LinearComponents` + `IntegrationSource` + `LinearComponentScore` fit and all three portal
snippets are executed by `tests/test_portal_snippets.py` against a fixture namespace, each
asserted to return a result object; `docs/api.md` gained `execution=` in both task signatures and
an "Execution" section covering `ExecutionConfig`, the NumPy backend, remembered execution and
the per-call override. The two manuscript companion HTMLs moved to `agenticresearch/archive/`.

**Verified.** All green on the branch: `ruff check`, `ruff format --check`, `ty check src`,
the full pytest suite under X64 (478 passed), the float32 leg (4 passed), `uv build`,
`mkdocs build --strict`, `registry.py validate`, and `pnpm validate` in `website/`. The
`LinearProblem` grep returns only ADR 0005, CHANGELOG and planning text in this directory.

**Cut or left open.** Nothing from the packet was cut. The Door 3 portal snippet executes
unchanged; its `calibration="isotonic"` argument is provenance metadata, not a sklearn call.
No exception hierarchy, budget-parameter table or `fit_quantizer` split was attempted; those are
S3.

**The one thing the next session must know.** S2 and S3 are both unblocked and independent. S3
must start from a written spec in its packet before any code, and its bit-identity gate (golden
engine and backend conformance suites) is the reason the public API freeze precedes S4, S6 and
S8. S2 must never load the v8 manuscript body into the orchestrator; it works from
`agenticresearch/manuscripts/README.md` and registry dumps.
