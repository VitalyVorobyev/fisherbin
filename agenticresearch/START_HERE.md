# START_HERE prompt for a ScoreQuant research agent

Read `README.md`, then all canonical files in its prescribed order.

Your **first response** must contain only:

1. one paragraph restating the canonical research problem;
2. the distinction among finite assignment, empirical inductive quantizer, and population quantizer;
3. the distinction between full D, in-bin \(D_s\), and projected efficient-score D;
4. the six supported score/model-access regimes;
5. the required information-loss metrics and the true-vs-surrogate Fisher rule for an estimated score;
6. five already-established project results that you must not rediscover;
7. five highest-priority unresolved dependencies from `OPEN_PROBLEMS.md`;
8. one sentence explaining why a finite \(D_s\) optimum is not automatically deployable while a terminal D exchange solution currently is.

Do not attempt a theorem until this scope check is complete. Then follow `AGENT.md`.

Before selecting an open problem, use `CLAIMS.json` as a dependency graph and list the unresolved nodes blocking the proposed target.
