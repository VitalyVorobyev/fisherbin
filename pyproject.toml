[project]
name = "fisherbin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vitaly Vorobyev", email = "vit.vorobiev@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
fisherbin = "fisherbin:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
